{
  "name": "@agent_tts/agentbox-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test $(find src -name '*.test.ts')",
    "lint": "eslint src --max-warnings 0"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
